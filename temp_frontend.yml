  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_URL: http://localhost:8001/api/v1
    container_name: projextpal-frontend-prod
    restart: unless-stopped
    networks:
      - projextpal-network
    ports:
      - "8083:8083"
